position(uuid:"${positionUuid}") {
    uuid, name, type, code,
    organization { uuid, shortName, longName },
    person { uuid, name, rank },
    associatedPositions {
        uuid, name,
        person { uuid, name }
    },
    location { uuid, name }
}
